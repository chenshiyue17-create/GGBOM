# P10 Boss

最省 Token 调用：
`执行 P10_DIRECT_EXEC.md；唯一数据源 P10_DIRECT_SPEC.json；节点只按 P10_NODE_RECIPES.md；不要解释；直接创建/Compile/Save/PIE；最后只返回状态行。`

失败续跑：
`继续 P10，仅修复 BLOCKING_ERROR=<上次错误>；不要重做已PASS资产；修复后重跑当前Gate。`
